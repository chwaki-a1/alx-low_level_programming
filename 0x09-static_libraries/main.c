#include "main.h"

/**
 * main - displays quote
 * Return: where the quote 0
 */
int main(void)
{
_puts("\"At the end of the day, my goal was to be the best hacker\"\n");
_puts("\t - Kevin Mitnick");
return (0);
}
